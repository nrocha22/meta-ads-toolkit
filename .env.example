# Copia este archivo a .env y rellena el token.
# .env está en .gitignore — nunca lo commitees.

# System User Access Token (Meta Business Suite > System Users).
# Recomendado: token de System User (no expira), no token de usuario.
ACCESS_TOKEN=

# Opcional. Algunas operaciones del CLI lo requieren.
# BUSINESS_ID=
